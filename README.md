# CodeLab (Modular Version)

A standard web project structure version of CodeLab.

## 📂 File Structure

- `index.html` - The frontend markup
- `style.css` - The visual styling
- `script.js` - The frontend logic
- `server.js` - The Node.js backend (handles execution and serves the files above)

## 🚀 How to Run

1. **Install Node.js** (v14 or higher).
2. Place all files in the same folder.
3. Open a terminal in that folder.
4. Run the server:
   ```bash
   node server.js
